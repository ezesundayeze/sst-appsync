type Todo {
   id: ID!
   description: String!
   checked: Boolean!
}

type Query {
   todos: [ Todo ]
   todo(id: ID!): Todo
}

type Subscription {
   todos: [ Todo ]
   @aws_subscribe(mutations: ["createTodo"])
}
